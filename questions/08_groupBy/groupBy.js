function groupBy(arr, attr) {
let arr=[];
for(let i=0;i<arr.length;i++){
    arr.forEach(number => {
        let result=arr[i].length;
        return `${result:['','']}`;
    });
}
}

module.exports = { groupBy };
